const { guessProductionMode } = require("@ngneat/tailwind");

module.exports = {
    prefix: '',
    content: ['./projects/demo/src/**/*.{html,ts}'],
    theme: {
      extend: {},
    },
    plugins: [],
};
